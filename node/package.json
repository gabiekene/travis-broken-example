{
  "scripts": {
    "build": "npm install && npm run test",
    "test": "jasmine --config=spec/jasmine.json",
    "start": ""
  },
  "dependencies": {
    "redux": "^3.7.2"
  },
  "devDependencies": {
    "jasmine": "^2.6.0"
  }
}
